#pragma GCC optimize("Ofast,O3")
#pragma GCC target("avx,avx2")
#include "bits/stdc++.h"

using namespace std;
using ll = long long;

void solve() {

}

int main() {
    cin.tie(0)->sync_with_stdio(false);
    int q = 1;
    cin >> q;
    while (q--) {
        solve();
        cout << '\n';
    }
}